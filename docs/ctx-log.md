# Журнал замеров

Факт из транскриптов Claude Code, снимается `node scripts/ctx.mjs --log "<кирпич>"`.

- **BASE** — окно на первом ответе: цена «просто открыть проект», до первого слова дизайнера.
- **PEAK** — максимум занятого окна за сессию.
- **ЗАДАЧА** — сколько добавил последний запрос.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| v1 (для сравнения) | «пересобрать шаблон» в product-design-starter | 51 612 | 88 608 | +26 382 |

## Из чего складывается BASE

Статический подсчёт файлов v1 (AGENTS.md 2 400 + frontmatter скиллов 2 100 + команды 570 ≈ 5 000)
объясняет **десятую часть** реального BASE. Остальное — то, что в файлах репо не видно:

- системный промпт Claude Code,
- описания инструментов подключённых MCP-серверов (Atlassian Rovo — ~40 инструментов),
- список скиллов (20 шт.) и сабагентов (8 шт.) с описаниями,
- память проекта.

Вывод для плана: экономия только на прозе AGENTS.md — это борьба за 5% от BASE.
Разложить BASE на слагаемые и понять, чем из них мы вообще управляем, — задача К0.
| К0 · нулевая точка | что это за проект? | 58 081 | 64 378 | +6 297 |

## Плагины (замер `claude plugin details`, 2026-08-26)

| Плагин | Скиллов | Always-on | Худший on-invoke | Статус |
|---|---|---|---|---|
| figma 2.2.96 | 12 + MCP | ~2 069 | — (схемы MCP резолвятся в рантайме) | оставлен, нужен дизайнеру |
| frontend-design | — | — | — | был выключен |

`claude plugin details <name>` печатает инвентарь и прогноз стоимости — дешёвый способ
оценить любой плагин до установки. Always-on платится всегда, on-invoke — при каждом
срабатывании скилла: плагин может не давить на старт и при этом внезапно съесть окно посреди задачи.

## К0.5 · Инвентаризация BASE (2026-08-26)

Разбивка `/context` на пустом репо (BASE 58k, окно 1M, sonnet-5):

| Категория | Токенов | Доля |
|---|---|---|
| **System tools** | **32.2k** | 54% |
| Messages | 12.8k | 22% |
| System prompt | 9.8k | 17% |
| Skills | 4.9k | 8% |
| Memory files | 65 | ~0 |

Больше половины окна — **описания встроенных инструментов**. Для сравнения, весь кит v1
(AGENTS.md + 20 скиллов) — это ~5k. Мы собирались резать 5k, не замечая 32k рядом.

### Что сколько стоит (headless-замеры, повторы ±2 токена)

| Конфигурация | BASE |
|---|---|
| как есть | 49 949 |
| без MCP-серверов (`--strict-mcp-config`) | 47 936 |
| дизайнерский набор `--tools` + MCP, **без** ToolSearch | 50 665 |
| дизайнерский набор `--tools` + MCP, **с** ToolSearch | 26 150 |
| только `Read,Bash`, без MCP | 15 302 |

**ToolSearch** — механизм отложенной загрузки: с ним схемы MCP не сидят в контексте,
а подтягиваются по требованию. Без него сорок инструментов Atlassian грузятся целиком (+26k).
Сами MCP-серверы при живом ToolSearch стоят всего ~2.4k — на них экономить нечего.

### Рычаг, который едет с шаблоном

Набор инструментов задаётся только флагом `--tools` при запуске (поле `tools` в settings.json
игнорируется — проверено, 49 948). Зато **`env` в `.claude/settings.json` работает** и не зависит
от способа запуска:

| Выключатель | Экономия |
|---|---|
| `CLAUDE_CODE_USE_POWERSHELL_TOOL=0` (дублирует Bash) | −3 696 |
| `CLAUDE_CODE_DISABLE_ARTIFACT=1` | −6 123 |
| `CLAUDE_CODE_DISABLE_WORKFLOWS=1` | −7 900 |
| `CLAUDE_CODE_DISABLE_CRON=1` | −2 148 |
| `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS=1` | −2 403 — **не берём**, нужен фоновый dev-сервер |
| `CLAUDE_CODE_DISABLE_BUNDLED_SKILLS=1` | −432 — не берём, ради этого терять `/code-review` глупо |

**Итог: 49 949 → 32 082 (−17 867, −36%)** без потери нужного дизайнеру.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К0.5 · settings.env | headless `ok` | 32 082 | — | — |

## К1 · AGENTS.md-минимум (2026-08-26)

Проверка в интерактивной сессии VS Code после К0.5 — настройки из репо применились сами:

| Категория | было | стало |
|---|---|---|
| System tools | 32.2k | 12.5k |
| Skills | 4.9k | 4.1k |
| System prompt | 9.8k | 9.6k |
| **BASE сессии** | **58.1k** | **26.3k** |

Цена самого кита:

| Шаг | BASE (headless) | Дельта |
|---|---|---|
| пустой репо, после К0.5 | 32 082 | — |
| + `AGENTS.md` (1084 б) без `CLAUDE.md` | 32 152 | +70 — **файл не читается** |
| + `CLAUDE.md` → `@AGENTS.md` | 32 598 | **+446 — вот теперь читается** |

`AGENTS.md` сам по себе Claude Code не подхватывает: нужен `CLAUDE.md` с импортом.
Проверка поведением: на вопрос «какой первый вопрос ты обязан задать» headless-сессия
ответила «Что вы хотите сделать?» — правило из файла работает.

Итого весь кит на этом шаге — **446 токенов**, три режима входа включительно.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К1 · AGENTS.md | headless `ok` | 32 598 | — | — |
| К1.1 · CLAUDE.md без @AGENTS.md-импорта | привет, давай поработаем | 32 326 | 48 089 | +15 763 |

### К1 пересобран по образцу xsolla-super-app

Там `AGENTS.md` лежит в `docs/architecture/` и помечен «do not read end-to-end for routine tasks»,
роутер `__ai/ROUTER.md` читается «when the task path is unclear» — always-on остаётся только `CLAUDE.md`.
Я же сначала импортировал `AGENTS.md` в `CLAUDE.md` и тем прибил его в контекст навсегда.

| Вариант | BASE | Цена кита |
|---|---|---|
| `CLAUDE.md` → `@AGENTS.md` (импорт) | 32 598 | 446 |
| `CLAUDE.md` 3 строки + `AGENTS.md` роутером по требованию | 32 319 | **237** |

Два урока из поведенческого теста (`claude -p "привет, давай поработаем"`):

1. **«Читай роутер, если путь неясен» не срабатывает.** Агент почти никогда не признаёт путь
   неясным: он не спросил про режим, а полез разбирать незакоммиченный diff. Правило первого
   хода должно жить в харнессе (SessionStart hook), а не в прозе.
2. **Агент правит машинные файлы.** Он удалил из `settings.json` строку, гасящую superpowers,
   решив, что она мёртвая — верно для этой машины, неверно для любого другого дизайнера.
   Нужен PreToolUse-guard на `.claude/**`, а не абзац про «машинные файлы».
3. Цена свободного хода: «привет, давай поработаем» — **+15 763 токена**, PEAK 48 089.
   Без явного первого шага агент читает всё подряд.

### К1.2 · Проза срезана до одной строки

Требование: «предельно минималистично, нещадно выпилить философию». Срезано всё, кроме
указателя на роутер. `CLAUDE.md` — 43 байта.

| Состояние | BASE |
|---|---|
| без `CLAUDE.md` вообще | 32 209 |
| `CLAUDE.md` в одну строку | 30 774 — **выброс, см. ниже** |
| прежний вариант, 3 правила | 32 319 |

> **Исправлено.** Из этих чисел был сделан вывод, что отсутствие `CLAUDE.md` дороже его наличия
> на 1 435 токенов. Перепроверка в одинаковых условиях (по два повтора): с файлом 32 373,
> без файла 32 324 — разница 49 токенов, ровно цена строки. Замер 30 774 был выбросом,
> вероятно, из-за не успевшего подключиться MCP-сервера. **Мерить всё повторами.**

Что снятие прозы НЕ сломало: агент и без правила «ты ведёшь» заканчивает ход предложением.

Что не работало и в прозе тоже (значит переносим в харнесс, а не возвращаем текстом):

| Правило | Как провалилось |
|---|---|
| «спроси про режим» | агент не спросил — путь ему всегда «ясен» |
| «не трогай машинные файлы» | агент отредактировал `.claude/settings.json` |
| «сначала детерминизм» | на вопрос о стоимости сессии парсил `.jsonl` руками вместо `scripts/ctx.mjs` |

Вывод: маршрутизация должна быть **структурной, а не текстовой** — `package.json` со
говорящими скриптами агент находит сам, в отличие от роутера, который надо догадаться прочитать.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К1.2 · одна строка | headless `ok` | 30 774 | — | — |

## К2 · Режим сессии — скиллом, а не прозой (2026-08-26)

Замечания дизайнера: (1) `ctx.mjs` — инструмент пересборки шаблона, дизайнеру не нужен,
в его контуре ему не место; (2) инструкция про начало сессии — не строка в AGENTS.md, а скилл.

Сделано: `scripts/ctx.mjs` → `tools/ctx.mjs` (к дизайнеру не поедет), `.claude/skills/start/`,
`state.json`, `CLAUDE.md`/`AGENTS.md` — по одной строке.

| Что | Цена always-on |
|---|---|
| скилл `start` (только описание) | **71** |
| `CLAUDE.md` одна строка | 49 |
| `AGENTS.md` одна строка | ~10 |
| **весь кит** | **~130** |

Поведенческий тест, `claude -p "хочу сделать экран профиля по макету из фигмы"`:
агент задал ровно один вопрос с тремя вариантами и не начал работу. **Правило, проваленное
в прозе, работает скиллом.** Он же сам сообщил, что Figma MCP не авторизован.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К2 · скилл start | headless «экран профиля из фигмы» | 32 373 | — | — |

### К2.1 · Проза удалена полностью

Замечание дизайнера: описания скиллов и так всегда в контексте — значит строка
«работа идёт через скилл start» в `CLAUDE.md`/`AGENTS.md` дублирует то, что агент уже видит.

Оба файла удалены. Поведенческий тест повторён без единой строки прозы:
на «хочу сделать экран профиля по макету из фигмы» агент вызвал скилл, задал один вопрос
с тремя вариантами и предположил подходящий. **Инструкции не понадобились вообще.**

Контур дизайнера: `.claude/settings.json`, `.claude/skills/start/SKILL.md`, `state.json`.
Постоянная стоимость кита — **71 токен** (одно описание скилла).

### К2.2 · Четвёртый режим и вопрос про дизайн-систему

Замечания дизайнера: нужен свободный вариант («свой способ работы»), и следующий шаг должен
**подкачивать модули под выбор** — дизайн-систему в том числе, а не тащить всё заранее.

`start` теперь задаёт два вопроса: режим (4 варианта, четвёртый — своими словами)
и дизайн-система (XUI / другая по ссылке / без неё).

| | Цена always-on |
|---|---|
| описание скилла до | 71 |
| описание скилла после | **97** |

Тест: «третий вариант — только экраны из фигмы» → записал `"mode": "figma-only"` в `state.json`
и задал второй вопрос. Значение режима агент придумал сам — словарь значений придётся
закрепить схемой, когда появится что проверять.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К2.2 · два вопроса | headless | 32 297 | — | — |

## К3 · Скелет прототипа — сделан и откачен (2026-08-26)

Был создан Vite + React скелет (7 файлов), поставлены зависимости, проверена сборка.
**Откачено по требованию дизайнера.** Причина важнее самого отката:

> Шаблон не содержит приложения. Приложение появляется тогда, когда дизайнер ответил
> на вопросы `start`, и агент подкачал ровно то, что нужно под его выбор. Класть скелет
> заранее — значит навязать стек тем, кто выбрал другой путь, и притащить `node_modules`
> тем, кому они сегодня не нужны.

И про метод: идти файл за файлом, согласовывая каждый **до** создания, а не предъявлять
семь готовых файлов и сборку постфактум.

Контур дизайнера остаётся прежним: `.claude/settings.json`, `.claude/skills/start/SKILL.md`,
`state.json`. Постоянная стоимость — 97 токенов.

## К4 · Инициализация приложения — скриптом, не прозой (2026-08-26)

Выбран вариант C: в шаблоне нет приложения, файлы появляются под конкретную задачу.
Знание «что нужно, чтобы показать экран» живёт в `scripts/init.mjs` (129 строк), а не в инструкциях.

`node scripts/init.mjs <screen>`:

- читает `state.json` → `designSystem.kind` (`xui` | `custom` | `none`);
- создаёт недостающее: `package.json`, `.gitignore`, `vite.config.ts`, `index.html`,
  `src/main.tsx`, `src/screens/<screen>/screen.tsx`;
- существующее не трогает — запускать можно сколько угодно раз;
- ставит зависимости один раз; если приватный реестр XUI недоступен — говорит об этом
  понятной фразой, а не ошибкой npm;
- при втором экране предупреждает, что `main.tsx` рендерит другой.

Дизайн-система меняет только `main.tsx` (обёртка `XUIProvider` для `xui`) и список зависимостей.
Содержимое экрана скрипт не выдумывает — это работа агента под задачу.

Скилл `start` вместо инструкций содержит одну строку:
«Then run `node scripts/init.mjs <screen>`».

Проверено: обе ветки (`none`, `xui`), повторный запуск, создание `.gitignore`.
Постоянная стоимость кита не изменилась — **97 токенов**, скрипт в контекст не грузится.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К4 · init.mjs | headless `ok` | 32 264 | — | — |

## К5 · Первый сквозной прогон (2026-08-26)

Чистая копия шаблона — четыре файла, как у дизайнера. Два сообщения:
«сделай экран профиля» → «2 и 3» (прототип сейчас, без дизайн-системы).

| Шаг | Стоил | Пик окна |
|---|---|---|
| открыть проект (BASE) | 32 306 | — |
| запрос → два вопроса | +2 009 | 34 315 |
| ответ → init, вёрстка, проверка в браузере | +17 653 | **52 718** |

**Главное сравнение: в v1 дизайнер платил 51 612 только за то, чтобы открыть проект.
Здесь за те же ~52 700 он получает готовый проверенный экран.**

Что сработало само:

- агент задал оба вопроса одним сообщением, не растягивая на два хода;
- `state.json` заполнен верно: `mode: prototype-first`, `feature: profile`, `debt: ["docs"]`
  — долг зафиксирован без единого напоминания в прозе;
- `init.mjs` создал каркас, агент дописал `screen.css` под задачу — ровно то разделение,
  ради которого скрипт не выдумывает содержимое;
- зависимости остались чистыми: `react`, `react-dom`, `vite`, плагин. Playwright,
  которым агент проверял результат в браузере, в проект не попал.

Что подсказал лог:

- агент пробовал вызвать скилл `run` (его нет — привычка из v1) и обошёлся `npm run dev`;
- ветка XUI ещё не проверена: приватный реестр может оказаться отдельной историей.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К5 · сквозной прогон | «сделай экран профиля» → «2 и 3» | 32 306 | 52 718 | +17 653 |

## К6 · Справочник по дизайн-системе (2026-08-26)

Прогон К5 показал: экран на XUI стоит в 5.7 раза дороже, чем без дизайн-системы, потому что
агент выясняет состав библиотеки чтением служебных файлов — 31 358 токенов на `Read`
и установка тринадцати пакетов по одному.

`scripts/ds-catalog.mjs` собирает опись из **установленных** пакетов: компонент, пакет,
пропсы с допустимыми значениями. Кладёт в `.claude/skills/xui/SKILL.md` — то есть это
сразу скилл: описание висит в контексте всегда (~50 токенов), тело грузится при работе с UI.
Устареть не может: обновилась библиотека — пересобрали.

Иконки (733 из 803 «компонентов») свёрнуты в одну строку — иначе опись весит 19.8 КБ вместо 6.9.

`init.mjs` теперь ставит базовый набор из 16 пакетов сразу и сам зовёт генератор.

| | без справочника | **со справочником** |
|---|---|---|
| задача «экран профиля на XUI» | +100 545 | **+64 403** |
| пик окна | 135 144 | **100 294** |
| чтение файлов библиотеки | 31 358 | 13 338 |

**Минус 36%.** Остаток — поведение, которого в типах не видно. Агент сам нашёл три штуки:
провайдер стартует в тёмной теме, `Avatar.text` — это инициалы, `Badge` в малых размерах
рендерится точкой без текста.

Отсюда второй слой: `notes.md` рядом со справочником — генератор создаёт его один раз
и больше не трогает, а агент дописывает туда каждую находку. Справочник знает, что есть;
заметки знают, обо что спотыкаются. Каждый следующий экран дешевле предыдущего.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К6 · справочник XUI | «экран профиля» → «2 и 1» | 30 843 | 100 294 | +64 403 |

## К7 · Три хука: правила, которые исполняются (2026-08-28)

Четыре правила проваливались в прозе. Переведены в харнесс — команды, которые запускает
сама программа в нужный момент, мимо решения агента.

| Хук | Событие | Что делает |
|---|---|---|
| `session-start.mjs` | сессия началась | читает `state.json` и кладёт строку состояния прямо в контекст; молчит, пока работа не начата |
| `notes-gate.mjs` | агент закончил отвечать | лазил внутрь пакетов и не записал находку — ход не завершается |
| `guard.mjs` | перед записью файла | запрещает править `.claude/**` и `scripts/**`; `notes.md` и всё в `src/` разрешено |

### Что пришлось чинить по дороге

**Ложное «уже записал».** Первая версия гейта сравнивала время изменения `notes.md` с началом
сессии. Но файл создаёт генератор справочника — в той же сессии, значит время всегда свежее,
и гейт молчал. Заменено на счёт записей: `session-start` снимает, сколько находок было на старте,
гейт сравнивает. Заодно генератор больше не наполняет `notes.md` заготовками — известные
особенности переехали в сам справочник, а заметки остались чисто агентскими.

**Ложное «копался в библиотеке».** Условие ловило `node_modules` и `@xsolla` по отдельности —
а они есть в любом импорте и в выводе установки. Гейт сработал на сессии, где агент только
запускал сервер. Ужесточено до реального пути внутрь пакета. Проверено на двух сохранённых
разговорах: там, где копался, — срабатывает; где не копался — молчит.

**Двусмысленные вопросы.** Агент разобрал «2 и 1» наоборот, потому что пронумеровал первый
список буквами, а второй словами. Скилл теперь требует цифры в обоих и подтверждение выбора
одной строкой до начала работы.

### Прогон

| | без хуков (К6) | **с хуками** |
|---|---|---|
| задача «экран профиля на XUI» | +64 403 | **+55 844** |
| пик окна | 100 294 | **90 802** |
| находок записано | 0 | **4** |

Дешевле на 13% — известные особенности теперь в справочнике, агент их не переоткрывает.
И это при том, что гейт добавляет один ход на дозапись.

Записанное агентом (не отписки, проверено глазами): обязательный `aria-label` у `IconButton`;
`xui-layout` вопреки названию не даёт примитивов раскладки, а реэкспортирует другие пакеты;
у `List.Row` нет слотов под иконку и шеврон; токены темы для своих обёрток берутся через
`useResolvedTheme().theme`.

Проверка защиты вживую: на «убери из settings.json отключение workflows» агент ответил отказом,
объяснил, что файл едет всем дизайнерам, и предложил решить осознанно. Файл не изменился.

| Кирпич | Сценарий | BASE | PEAK | ЗАДАЧА |
|---|---|---|---|---|
| К7 · хуки | «экран профиля» → «2 и 1» | 31 949 | 90 802 | +55 844 |

### Второй экран в том же проекте — накопление работает

| | без хуков (К6) | с хуками (К7) |
|---|---|---|
| первый экран | +64 403 | +55 844 |
| второй экран | +56 417 (−12%) | **+42 223 (−24%)** |
| пик окна | 88 431 | 74 337 |
| ходов агента | 90 | 70 |

Экономия на повторе выросла вдвое: раньше находки терялись вместе с разговором, теперь лежат
в проекте. Записано ещё две, обе с деталями: в версии 0.213 нет Switch / Checkbox / Radio
(проверено в трёх пакетах, указано, из каких токенов собирать свой), и `useResolvedTheme()`
отдаёт `setMode` для живого переключения темы.

Путь целиком: первый экран на XUI без справочника стоил **100 545**, второй экран сегодня —
**42 223**. Разница в 2.4 раза, и вся она в том, что знание перестало испаряться.

## К8 · Несколько экранов (2026-08-28)

Прогоны вскрыли дыру: каждый новый экран **заменял** предыдущий — агент дважды сообщал,
что «main.tsx теперь показывает другой экран». Для работы дизайнера это неприемлемо.

Решение не в том, чтобы скрипт аккуратнее правил точку входа, а в том, чтобы **не править её
вообще**. `src/app.tsx` находит экраны сам: каждая папка в `src/screens/` — это экран,
регистрировать ничего не нужно. Создали папку — экран в списке.

- переход между экранами — по адресу вида `#settings`, без единой зависимости;
- переключатель экранов виден **только на дев-сервере** — в собранной версии его нет,
  проверено: в бандле оба экрана есть, разметки переключателя нет;
- `init.mjs` при добавлении второго экрана точку входа больше не трогает, и предупреждение
  «main.tsx рендерит другой экран» удалено за ненадобностью.

Проверено на чистом проекте: два экрана, сборка 76 мс.

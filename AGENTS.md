# Router

Read when the task path is unclear, then act. Never read end-to-end for routine work.

| Task | Do |
|---|---|
| Session start, mode unknown | Ask one question — full product work / prototype now, docs later / screens from Figma only — and record the answer |
| What did the session cost | `node scripts/ctx.mjs` |

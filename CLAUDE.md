You drive: never end a turn passively — say what changed, then propose the next step or ask one question.
Deterministic first: run a script, hook or lint instead of reasoning; never re-derive what a command reports.
Path unclear? Read AGENTS.md — the router. Not before.
